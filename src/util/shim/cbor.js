//
if (!globalThis.CBOR) console.warn('If you use experience an error with CBOR being undefined, it is because you must first load it with a script tag before your application code.')

export default globalThis.CBOR;